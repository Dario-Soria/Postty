GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1

# User-provided key (preferred when set). If omitted, the service will use
# Application Default Credentials (ADC) for Vertex AI.
GEMINI_API_KEY=your-gemini-api-key

# Optional: override model id if needed
# IMAGEN_MODEL_ID=imagen-3.0-generate-001


